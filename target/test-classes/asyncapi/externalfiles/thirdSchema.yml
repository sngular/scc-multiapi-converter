components:
  schemas:
    OrderLine:
      type: object
      properties:
        order:
          $ref: '#/components/schemas/Order'

    Order:
      type: object
      properties:
        companyName:
          type: string
          example: Corunet